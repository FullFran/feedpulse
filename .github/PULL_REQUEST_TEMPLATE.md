## Summary

<!-- What changes and why. One or two sentences. -->

Closes #

## Type of change

- [ ] Bug fix
- [ ] New feature
- [ ] Refactor (no behaviour change)
- [ ] Documentation
- [ ] Build, CI or tooling

## Evidence

- [ ] `npm run lint` passes
- [ ] `npm run format:check` passes
- [ ] `npm run typecheck` passes
- [ ] `npm test` passes with `TEST_DATABASE_URL` set
- [ ] `npm run smoke:ci` passes (only if this touches the Dockerfile, Compose files, `src/main/`, or a migration)

## Notes for the reviewer

<!-- Anything that is not obvious from the diff: a rejected alternative, a
     migration that deletes rows, a breaking API change. Delete if empty. -->

<!-- New SQL migration? It must be a new file continuing the sequence.
     Never edit a migration that has already run — see CONTRIBUTING.md. -->
